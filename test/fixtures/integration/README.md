# Integration smoke fixtures

Curated packages for `pnpm test:integration-writes:smoke`.

| File | Role |
| ---- | ---- |
| `smoke.pptx` | Blank 16:9 deck (from `assets/default.pptx`) |
| `smoke-doc.docx` | Blank Word doc (from `assets/default.docx`) |

Sibling workspaces (`smoke/`, `smoke-doc/`) and summary JSON are gitignored.
